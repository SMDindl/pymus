# Test file to see if dependencies are installed correctly
# and if the code runs without errors.
import spotipy
from spotipy.oauth2 import SpotifyOAuth
import pylast
import os

print("Good! All dependencies are installed correctly.")